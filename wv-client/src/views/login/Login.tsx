import React from 'react';

import * as logger from 'utils/logger';

const Login: React.FC = () => {
  logger.rendering();

  return (
    <div>
      <p>Login</p>
    </div>
  );
};

export default Login;
